## AI知识库管理平台 PRD

### 一、产品概述

#### 1.1 产品定位
企业级知识库管理平台后端系统，支持智能文档处理、灵活的权限控制和API服务能力。
总结来说，是一个支持多用户的知识库后端，支持知识库创建、文档上传解析、检索的能力，并支持基于api_key的知识库搜索接口，是一个RAG系统必不可少的一个部分。

#### 1.2 核心价值
- **智能文档处理**：支持多格式文档解析，包括OCR识别
- **灵活分块策略**：内置多种分块策略
- **高效检索**：基于向量的语义搜索
- **开放API**：支持针对知识库搜索的API服务
- **私有化部署**：基于Docker的完整部署方案

#### 1.3 技术栈

| 组件类别 | 技术选型 | 版本/说明 |
|---------|---------|----------|
| **后端框架** | FastAPI | 异步高性能框架 |
| **数据库** | PostgreSQL + pgvector + paradedb | 关系型数据库 + 向量扩展 + 全文搜索扩展 |
| **对象存储** | MinIO | S3兼容的开源对象存储 |
| **异步任务** | Taskiq + Redis | 分布式任务队列 |
| **Embedding** | BGE M3 | 1024维 |
| **OCR解析** | MinerU / PaddleOCR | 开源文档解析工具 |
| **日志** | fastapi-radar dashbard / loguru |  |
| **部署** | Docker + Docker Compose | 容器化部署 |

---

### 数据模型和接口设计

#### 2.1 核心数据模型

基于业务需求，系统设计了以下核心数据实体：

##### 2.1.1 用户管理 (User)
- **用户身份**: 支持多用户系统，每个用户拥有独立的数据空间
- **基本信息**: 用户名、邮箱、密码、头像、全名
- **配置管理**: JSONB格式存储用户个性化设置
- **权限控制**: 超级用户标志、激活状态
- **统计信息**: 存储使用量、API调用次数
- **关联关系**: 用户拥有的知识库、上传的文档、API密钥等

##### 2.1.2 知识库管理 (Collection)
- **知识库概念**: 用户可创建多个知识库，每个知识库是独立的文档集合
- **基本信息**: 名称、描述、图标URL
- **状态管理**: 活跃/归档状态控制
- **统计信息**: 文档数量、总大小、分块数量、最后更新时间
- **灵活配置**: JSONB格式存储知识库级设置（如分块策略、Embedding配置等）
- **数据隔离**: 严格按用户ID隔离，确保数据安全

##### 2.1.3 文档管理 (Document)
- **多格式支持**: PDF、DOCX、TXT、HTML、XLSX等格式
- **文件存储**: 使用MinIO对象存储，支持大文件处理
- **处理流程**: 待处理→处理中→已完成→失败，支持进度跟踪
- **内容提取**: 自动提取文本内容并转换为Markdown格式
- **OCR集成**: 支持图片型PDF的OCR文字识别
- **分块配置**: 支持文档级自定义分块策略，覆盖知识库默认设置

##### 2.1.4 文档分块 (DocumentChunk)
- **智能分块**: 支持多种分块策略（段落、句子、固定长度等）
- **向量存储**: 1024维BGE-M3向量，支持语义搜索
- **内容优化**: 支持生成块摘要，提升检索效果
- **层级结构**: 支持父子块关系，实现层级分块
- **索引优化**: 支持HNSW

##### 2.1.5 API密钥管理 (APIKey)
- **密钥安全**: 不存储明文密钥，仅保存哈希值
- **权限控制**: 基于scope的细粒度权限管理（搜索、上传、读取、删除）
- **配额管理**: 支持分钟级、日级、月级调用配额
- **使用统计**: 实时统计API调用次数，支持配额预警
- **安全策略**: 支持IP白名单、过期时间等安全配置
- **密钥展示**: 前后缀显示，便于识别而不泄露完整密钥

##### 2.1.6 使用日志 (APIUsageLog)
- **完整记录**: 记录每次API调用的详细信息
- **性能监控**: 记录响应时间，支持性能分析
- **错误追踪**: 记录错误信息和堆栈，便于问题定位
- **安全审计**: 记录IP地址、用户代理等上下文信息
- **数据分析**: 支持调用模式分析、用户行为分析

#### 2.2 接口设计原则

##### 2.2.1 RESTful API设计
- **版本控制**: 使用/api前缀进行版本管理
- **资源导向**: 以资源为中心设计URL结构
- **HTTP方法**: 严格遵循GET/POST/PUT/DELETE语义
- **状态码**: 使用标准HTTP状态码表示操作结果

##### 2.2.2 数据验证与序列化
- **Pydantic模型**: 使用Pydantic进行请求/响应数据验证
- **类型安全**: 全面使用Python类型提示
- **自动文档**: 基于OpenAPI自动生成API文档
- **错误处理**: 统一的错误响应格式

##### 2.2.3 安全设计
- **身份认证**: JWT Token认证机制
- **权限控制**: 基于角色的访问控制(RBAC)
- **数据隔离**: 所有查询必须包含用户ID过滤
- **输入验证**: 严格的输入数据验证和清理
- **速率限制**: 基于API密钥的调用频率限制

##### 2.2.4 异步处理
- **异步API**: 所有API端点支持异步处理
- **后台任务**: 使用Taskiq处理文档解析、向量生成等耗时操作
- **进度跟踪**: 支持异步任务的实时进度查询
- **结果通知**: 支持任务完成后的回调通知机制

#### 2.3 核心接口列表

##### 2.3.1 用户管理接口
- `POST /api/v1/auth/register` - 用户注册
- `POST /api/v1/auth/login` - 用户登录
- `GET /api/v1/users/profile` - 获取用户信息
- `PUT /api/v1/users/profile` - 更新用户信息
- `POST /api/v1/auth/change-password` - 修改密码

##### 2.3.2 知识库管理接口
- `GET /api/v1/collections` - 获取知识库列表
- `POST /api/v1/collections` - 创建知识库
- `GET /api/v1/collections/{id}` - 获取知识库详情
- `PUT /api/v1/collections/{id}` - 更新知识库
- `DELETE /api/v1/collections/{id}` - 删除知识库
- `POST /api/v1/collections/{id}/archive` - 归档知识库

##### 2.3.3 文档管理接口
- `GET /api/v1/collections/{kb_id}/documents` - 获取文档列表
- `POST /api/v1/collections/{kb_id}/documents/upload` - 上传文档(支持批量)
- `GET /api/v1/documents/{id}` - 获取文档详情
- `DELETE /api/v1/documents` - 删除文档(支持批量)
- `POST /api/v1/documents/process` 解析文档(支持批量)
- `POST /api/v1/documents/reprocess` - 重新解析文档
- `PUT /api/v1/dociments/{id}` 更新解析配置
- `GET /api/v1/documents/{id}/status` - 查询处理状态

- `GET /api/v1/documents-chunks/{id}` - 获取文档分块列表

##### 2.3.4 搜索接口
- `POST /api/v1/search` - 搜索(支持多知识库、搜索参数、支持api_key等)

##### 2.3.5 API密钥管理接口
- `GET /api/v1/api-keys` - 获取API密钥列表
- `POST /api/v1/api-keys` - 创建API密钥
- `DELETE /api/v1/api-keys/{id}` - 删除API密钥
- `GET /api/v1/api-keys/{id}/usage` - 获取使用统计

